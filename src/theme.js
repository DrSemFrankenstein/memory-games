// theme.js
import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#455a64", // Replace with your primary color
    },
    secondary: {
      main: "#646cff", // Replace with your secondary color
    },
    // ... Add more customizations as you see fit
  },
});

export default theme;
